# Second virtual gamepad from the GPIO daemon. Only exists so the BTN 5 + BTN 6
# chord (player2_start) reaches MAME as 2-Player Start on RetroArch port 2.
# Button order matches GAMEPAD2_CAPS in src/gpio_daemon.py: [BTN_SOUTH, BTN_START].
input_device = "Pi Arcade Controller P2"
input_driver = "udev"
input_a_btn = "0"
input_start_btn = "1"
